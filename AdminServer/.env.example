# Render provides PORT automatically.
PORT=3002
NODE_ENV=development

# Required in production. Send it from the admin client as the X-Admin-Token header.
ADMIN_TOKEN=change-me

# Comma-separated admin client origins allowed to call this server.
CLIENT_ORIGIN=http://localhost:5174
